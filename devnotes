To duplicated dev environment exactly (on Mac):

Install xcode (might come with git, if not get git via homebrew)

Install VS code
Install VS code python extension

Install Homebrew
Install depndencies of pyenv: brew install openssl readline sqlite3 xz zlib
Install pyenv: brew install pyenv
Install pyenv virtualenv script: brew install pyenv-virtualenv
Modify .bash_profile (or ) with (needs shell restart after adding):
    export PYENV_ROOT="$HOME/.pyenv"
    export PATH="$PYENV_ROOT/bin:$PATH"
    eval "$(pyenv init -)"
    eval "$(pyenv virtualenv-init -)"
Install python dev version 3.7.6: pyenv install -v 3.7.6
cd to dev directory
Make dev version of python (3.7.6) local for dev directory (must be run in dev dir): pyenv local 3.7.6
Create virtual environment project: pyenv virtualenv 3.7.6 NLoed
Make environment local dev dir (must be run in dev dir): pyenv local NLoed
Install Casadi and Numpy (must be run in dev dir): pip install casadi, pip install numpy

Git clone repo into dev dir: git clone https://github.com/NateBraniff/NLoed

Install nloed in edit mode (mist run in dev dir): pip install -e .


